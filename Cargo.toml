[workspace]
members = ["crates/*"]
exclude = ["crates/language-server/editors/zed"]
resolver = "2"

[profile.dev.package.solc]
opt-level = 3

[profile.dev]
# Set to 0 to make the build faster and debugging more difficult.
debug = 1
