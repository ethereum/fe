use core::{Option, panic}

fn f() -> u32 {
    let x = Option::Some(10)
    x.unwrap()

    let y: Option<u8> = Option::none()
    let z: Option<u8> = Option::default()

    match x {
        Option::Some(x) => x
        Option::None => panic()
    }
}
