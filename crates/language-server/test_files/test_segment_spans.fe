mod nested {
    pub const NESTED_CONST: u32 = 100
}

use nested::NESTED_CONST as NC

fn test_segments() {
    // Path expression with segments - should have segment spans
    let a = nested::NESTED_CONST
    
    // Use path - does this have segment spans?
    let b = NC
}