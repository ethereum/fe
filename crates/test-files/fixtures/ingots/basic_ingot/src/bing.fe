use std::error::Error
use std::context::Context

pub struct Bing {
    pub my_address: address
}

pub fn get_42_backend() -> u256 {
    return std::evm::add(21, 21)
}

pub contract BingContract {
    pub fn add(self, _ x: u256, _ y: u256) -> u256 {
        return x + y
    }
}
