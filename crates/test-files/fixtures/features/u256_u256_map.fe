contract Foo {
    bar: Map<u256, u256>

    pub fn read_bar(self, key: u256) -> u256 {
        return self.bar[key]
    }

    pub fn write_bar(mut self, key: u256, value: u256) {
        self.bar[key] = value
    }
}
