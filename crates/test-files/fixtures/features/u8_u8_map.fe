contract Foo {
    bar: Map<u8, u8>

    pub fn read_bar(self, key: u8) -> u8 {
        return self.bar[key]
    }

    pub fn write_bar(mut self, key: u8, value: u8) {
        self.bar[key] = value
    }
}
