contract Foo {
    pub fn bar(x: u256, y: u256, z: u256) -> Array<u256, 3> {
        let mut my_array: Array<u256, 3> = [0; 3]
        my_array[0] = x
        my_array[1] = y
        my_array[2] = z
        return my_array
    }
}
