contract Foo {
    some_thing: i8
    some_other: i8

    pub fn bar(mut self, val: i8) -> i8 {
        self.some_thing = val
        self.some_other = val
        let x: i8 = self.some_thing + self.some_other
        return x
    }
}
