contract Foo {
    bar: Map<u16, u16>

    pub fn read_bar(self, key: u16) -> u16 {
        return self.bar[key]
    }

    pub fn write_bar(mut self, key: u16, value: u16) {
        self.bar[key] = value
    }
}
