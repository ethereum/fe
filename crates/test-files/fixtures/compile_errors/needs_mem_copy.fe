contract Foo {
    my_sto_array: Array<u256, 10>

    pub fn bar(self, mut my_array: Array<u256, 10>) {
        my_array = self.my_sto_array
        let x: Array<u256, 10> = self.my_sto_array
    }

    pub fn foo(self) -> Array<u256, 10> {
        return self.my_sto_array
    }
}
