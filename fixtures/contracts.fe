use std::spec

contract Foo {
    pub fn get_42(self) -> u256 {
        return 42
    }
}

#invariant
fn verify_get_42(mut ctx: Context) {
    let foo: Foo = Foo.create(ctx, 0)
    spec::given_eq(u256(address(foo)), 26)
    spec::assert_eq(foo.get_42(), 42)
}
